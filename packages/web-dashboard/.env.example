# SAFT Enhanced Configuration
# Path to the SAFT binary (build with: cargo build --release --package saft-enhanced)
SAFT_BINARY_PATH=/path/to/polcacadot/target/release/saft

# Temporary directory for uploaded files
TEMP_UPLOAD_DIR=/tmp/saft-uploads

# Environment (development|production)
NODE_ENV=development
